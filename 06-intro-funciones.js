// Definición de funciones
                 // Parámetro
function siguiente (n) {
    return n + 1;
}

function doble (n) {
    return n * 2;
}

// Programa principal
                             // Argumento
let resultado = doble(siguiente(4));
console.log(resultado);